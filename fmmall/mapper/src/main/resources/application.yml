spring:
  datasource:
    druid: # 添加阿里巴巴druid连接池
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/fmmall?characterEncoding=utf-8
      username: root
      password: root

mybatis:
#  mapper位置
  mapper-locations: classpath:mappers/*Mapper.xml
#  别名
  type-aliases-package: com.qlz.fmmall.entity
