// 用于存放数据
// 比如：登录后，将数据存放在此处，或者页面跳转/路由时，存放要传递的数据/参数

import { createStore } from 'vuex'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
